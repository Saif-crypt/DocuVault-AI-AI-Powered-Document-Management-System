# =============================================================================
# AI_Module — Python Dependencies
# =============================================================================
# Install with:   pip install -r requirements.txt
#
# System dependencies (install separately via your OS package manager):
#   • Tesseract OCR  →  apt install tesseract-ocr            (Linux)
#                    →  brew install tesseract               (macOS)
#                    →  Download from https://github.com/UB-Mannheim/tesseract/wiki  (Windows)
#   • Poppler        →  apt install poppler-utils            (Linux)
#                    →  brew install poppler                 (macOS)
#                    →  Download from https://www.gyan.dev/extras/pdf/poppler-utils.html (Windows)
# =============================================================================

pytesseract>=0.3.10   # Python wrapper around the Tesseract OCR binary
Pillow>=10.0.0        # Image loading, saving, and manipulation
opencv-python>=4.8.0  # Image preprocessing (grayscale, thresholding, resize)
